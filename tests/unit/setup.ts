// fallow-ignore-file unused-file
/**
 * Test setup for playwright package.
 *
 * The patch to @cloudflare/playwright makes the cloudflare:workers import lazy,
 * so no mocking is needed. This file exists for future test configuration if needed.
 */
